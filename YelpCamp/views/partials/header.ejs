<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>YelpCamp</title>
    <!-- CSS Style -->
    <link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js" integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous"></script>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
</head>
<!-- NAVBAR -->
<body>
<nav class="navbar navbar-expand-sm navbar-dark border border-success"> 
    <a class="navbar-brand" href="/">YelpCamp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <% if (locals.user){ %>
                <li class="nav-item">
                    <span class="nav-link active" >Welcome, <%= locals.user.username %></span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Logout</a>
                </li>
            <% }else{ %>
                <li class="nav-item">
                    <a class="nav-link" href="/register">Sign In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>
<!-- FLASH MESSAGES -->
<% if (locals.errorMessage.length > 0){ %>
    <div class="alert-danger border border-danger rounded text-center m-auto px-2" role="alert">
    <%= locals.errorMessage %>
    </div>
<% }; %>

<% if (locals.successMessage.length > 0){ %>
    <div class="alert-success border border-success rounded text-center m-auto px-2" role="alert">
    <%= locals.successMessage %>
    </div>
<% }; %>
